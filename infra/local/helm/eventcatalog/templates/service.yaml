apiVersion: v1
kind: Service
metadata:
  name: {{ include "eventcatalog.fullname" . }}
  labels:
    {{- include "eventcatalog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eventcatalog.selectorLabels" . | nindent 4 }}
