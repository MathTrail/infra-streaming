apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: centrifugo-vault-auth
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: centrifugo-vault-role
    serviceAccount: default
