minio:
  mode: standalone

  existingSecret: "minio-root-creds"

  persistence:
    enabled: true
    size: 20Gi

  buckets:
    - name: automq-data
      policy: none
    - name: risingwave-data
      policy: none

  resources:
    requests:
      memory: 256Mi
      cpu: 100m
