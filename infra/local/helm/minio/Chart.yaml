apiVersion: v2
name: minio-local
description: MinIO local wrapper for MathTrail streaming namespace
type: application
version: 0.2.0
dependencies:
  - name: minio
    version: "5.4.0"
    repository: "https://charts.min.io/"
