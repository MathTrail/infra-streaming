apiVersion: v2
name: minio
description: MinIO standalone — S3-compatible storage for Flink checkpoints (local dev)
version: 0.1.0
dependencies:
  - name: minio
    version: "*"
    repository: "https://MathTrail.github.io/charts/charts"
