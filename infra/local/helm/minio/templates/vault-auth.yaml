apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: minio-vault-auth
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: minio-role
    serviceAccount: default
