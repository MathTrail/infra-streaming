apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: minio-oidc-creds
spec:
  type: kv-v2
  mount: secret
  path: local/streaming-minio-oidc
  destination:
    name: minio-oidc-creds
    create: true
  refreshAfter: 1h
  vaultAuthRef: minio-vault-auth
